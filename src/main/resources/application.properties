spring.application.name=sheerchat

# Database Configuration (automatically configured when using Docker Compose or Testcontainers)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Optional: Enable email confirmation (set to true if email service is configured)
app.email.confirmation.enabled=false
